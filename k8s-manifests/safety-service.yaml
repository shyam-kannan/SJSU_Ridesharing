apiVersion: v1
kind: ServiceAccount
metadata:
  name: safety-service-ksa
  namespace: lessgo
  annotations:
    iam.gke.io/gcp-service-account: safety-service-gsa@lessgo-492322.iam.gserviceaccount.com
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: safety-service
  namespace: lessgo
  labels:
    app: safety-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: safety-service
  template:
    metadata:
      labels:
        app: safety-service
        tier: backend
    spec:
      serviceAccountName: safety-service-ksa
      enableServiceLinks: false
      containers:
      - name: safety-service
        image: us-west1-docker.pkg.dev/lessgo-492322/microservices-images/safety-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: http
        envFrom:
        - configMapRef:
            name: lessgo-config
        - secretRef:
            name: lessgo-secrets
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: safety-service
  namespace: lessgo
  labels:
    app: safety-service
spec:
  selector:
    app: safety-service
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
    name: http
  type: ClusterIP
