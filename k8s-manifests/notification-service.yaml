apiVersion: v1
kind: ServiceAccount
metadata:
  name: notification-service-ksa
  namespace: lessgo
  annotations:
    iam.gke.io/gcp-service-account: notification-service-gsa@lessgo-492322.iam.gserviceaccount.com
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: lessgo
  labels:
    app: notification-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
        tier: backend
    spec:
      serviceAccountName: notification-service-ksa
      enableServiceLinks: false
      containers:
      - name: notification-service
        image: us-west1-docker.pkg.dev/lessgo-492322/microservices-images/notification-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3006
          name: http
        envFrom:
        - configMapRef:
            name: lessgo-config
        - secretRef:
            name: lessgo-secrets
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3006
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3006
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: lessgo
  labels:
    app: notification-service
spec:
  selector:
    app: notification-service
  ports:
  - protocol: TCP
    port: 3006
    targetPort: 3006
    name: http
  type: ClusterIP
