apiVersion: apps/v1
kind: Deployment
metadata:
  name: trip-service
  namespace: lessgo
  labels:
    app: trip-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trip-service
  template:
    metadata:
      labels:
        app: trip-service
        tier: backend
    spec:
      enableServiceLinks: false
      containers:
      - name: trip-service
        image: gcr.io/lessgo-490819/trip-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3003
          name: http
        envFrom:
        - configMapRef:
            name: lessgo-config
        - secretRef:
            name: lessgo-secrets
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: trip-service
  namespace: lessgo
  labels:
    app: trip-service
spec:
  selector:
    app: trip-service
  ports:
  - protocol: TCP
    port: 3003
    targetPort: 3003
    name: http
  type: ClusterIP
