apiVersion: v1
kind: ServiceAccount
metadata:
  name: trip-service-ksa
  namespace: lessgo
  annotations:
    iam.gke.io/gcp-service-account: trip-service-gsa@lessgo-492322.iam.gserviceaccount.com
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: trip-service
  namespace: lessgo
  labels:
    app: trip-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trip-service
  template:
    metadata:
      labels:
        app: trip-service
        tier: backend
    spec:
      serviceAccountName: trip-service-ksa
      enableServiceLinks: false
      containers:
      - name: trip-service
        image: us-west1-docker.pkg.dev/lessgo-492322/microservices-images/trip-service:${COMMIT_SHA}
        imagePullPolicy: Always
        ports:
        - containerPort: 3003
          name: http
        volumeMounts:
        - name: secrets-store
          mountPath: /mnt/secrets-store
          readOnly: true
        envFrom:
        - configMapRef:
            name: lessgo-config
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      volumes:
      - name: secrets-store
        csi:
          driver: secrets-store-gke.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: lessgo-secrets-provider
---
apiVersion: v1
kind: Service
metadata:
  name: trip-service
  namespace: lessgo
  labels:
    app: trip-service
spec:
  selector:
    app: trip-service
  ports:
  - protocol: TCP
    port: 3003
    targetPort: 3003
    name: http
  type: ClusterIP
