apiVersion: v1
kind: ServiceAccount
metadata:
  name: cost-calculation-service-ksa
  namespace: lessgo
  annotations:
    iam.gke.io/gcp-service-account: cost-calculation-service-gsa@lessgo-492322.iam.gserviceaccount.com
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cost-calculation-service
  namespace: lessgo
  labels:
    app: cost-calculation-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cost-calculation-service
  template:
    metadata:
      labels:
        app: cost-calculation-service
        tier: backend
    spec:
      serviceAccountName: cost-calculation-service-ksa
      enableServiceLinks: false
      containers:
      - name: cost-calculation-service
        image: us-west1-docker.pkg.dev/lessgo-492322/microservices-images/cost-calculation-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3009
          name: http
        volumeMounts:
        - name: secrets-store
          mountPath: /mnt/secrets-store
          readOnly: true
        envFrom:
        - configMapRef:
            name: lessgo-config
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3009
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3009
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      volumes:
      - name: secrets-store
        csi:
          driver: secrets-store-gke.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: lessgo-secrets-provider
---
apiVersion: v1
kind: Service
metadata:
  name: cost-calculation-service
  namespace: lessgo
  labels:
    app: cost-calculation-service
spec:
  selector:
    app: cost-calculation-service
  ports:
  - protocol: TCP
    port: 3009
    targetPort: 3009
    name: http
  type: ClusterIP
