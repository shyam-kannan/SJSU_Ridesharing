apiVersion: apps/v1
kind: Deployment
metadata:
  name: cost-calculation-service
  namespace: lessgo
  labels:
    app: cost-calculation-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cost-calculation-service
  template:
    metadata:
      labels:
        app: cost-calculation-service
        tier: backend
    spec:
      enableServiceLinks: false
      containers:
      - name: cost-calculation-service
        image: gcr.io/lessgo-490819/cost-calculation-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3009
          name: http
        envFrom:
        - configMapRef:
            name: lessgo-config
        - secretRef:
            name: lessgo-secrets
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3009
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3009
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: cost-calculation-service
  namespace: lessgo
  labels:
    app: cost-calculation-service
spec:
  selector:
    app: cost-calculation-service
  ports:
  - protocol: TCP
    port: 3009
    targetPort: 3009
    name: http
  type: ClusterIP
