apiVersion: v1
kind: ServiceAccount
metadata:
  name: payment-service-ksa
  namespace: lessgo
  annotations:
    iam.gke.io/gcp-service-account: payment-service-gsa@lessgo-492322.iam.gserviceaccount.com
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: lessgo
  labels:
    app: payment-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
        tier: backend
    spec:
      serviceAccountName: payment-service-ksa
      enableServiceLinks: false
      containers:
      - name: payment-service
        image: us-west1-docker.pkg.dev/lessgo-492322/microservices-images/payment-service:${COMMIT_SHA}
        imagePullPolicy: Always
        ports:
        - containerPort: 3005
          name: http
        volumeMounts:
        - name: secrets-store
          mountPath: /mnt/secrets-store
          readOnly: true
        envFrom:
        - configMapRef:
            name: lessgo-config
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      volumes:
      - name: secrets-store
        csi:
          driver: secrets-store-gke.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: lessgo-secrets-provider
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: lessgo
  labels:
    app: payment-service
spec:
  selector:
    app: payment-service
  ports:
  - protocol: TCP
    port: 3005
    targetPort: 3005
    name: http
  type: ClusterIP
