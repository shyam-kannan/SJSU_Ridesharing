apiVersion: v1
kind: ServiceAccount
metadata:
  name: booking-service-ksa
  namespace: lessgo
  annotations:
    iam.gke.io/gcp-service-account: booking-service-gsa@lessgo-492322.iam.gserviceaccount.com
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service
  namespace: lessgo
  labels:
    app: booking-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: booking-service
  template:
    metadata:
      labels:
        app: booking-service
        tier: backend
    spec:
      serviceAccountName: booking-service-ksa
      enableServiceLinks: false
      containers:
      - name: booking-service
        image: us-west1-docker.pkg.dev/lessgo-492322/microservices-images/booking-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3004
          name: http
        volumeMounts:
        - name: secrets-store
          mountPath: /mnt/secrets-store
          readOnly: true
        envFrom:
        - configMapRef:
            name: lessgo-config
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      volumes:
      - name: secrets-store
        csi:
          driver: secrets-store-gke.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: lessgo-secrets-provider
---
apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: lessgo
  labels:
    app: booking-service
spec:
  selector:
    app: booking-service
  ports:
  - protocol: TCP
    port: 3004
    targetPort: 3004
    name: http
  type: ClusterIP
