apiVersion: v1
kind: ServiceAccount
metadata:
  name: grouping-service-ksa
  namespace: lessgo
  annotations:
    iam.gke.io/gcp-service-account: grouping-service-gsa@lessgo-492322.iam.gserviceaccount.com
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grouping-service
  namespace: lessgo
  labels:
    app: grouping-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grouping-service
  template:
    metadata:
      labels:
        app: grouping-service
        tier: backend
    spec:
      serviceAccountName: grouping-service-ksa
      enableServiceLinks: false
      containers:
      - name: grouping-service
        image: us-west1-docker.pkg.dev/lessgo-492322/microservices-images/grouping-service:${COMMIT_SHA}
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: http
        volumeMounts:
        - name: secrets-store
          mountPath: /mnt/secrets-store
          readOnly: true
        envFrom:
        - configMapRef:
            name: lessgo-config
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
      volumes:
      - name: secrets-store
        csi:
          driver: secrets-store-gke.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: lessgo-secrets-provider
---
apiVersion: v1
kind: Service
metadata:
  name: grouping-service
  namespace: lessgo
  labels:
    app: grouping-service
spec:
  selector:
    app: grouping-service
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
    name: http
  type: ClusterIP
