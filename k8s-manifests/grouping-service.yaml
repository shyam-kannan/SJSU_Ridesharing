apiVersion: apps/v1
kind: Deployment
metadata:
  name: grouping-service
  namespace: lessgo
  labels:
    app: grouping-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grouping-service
  template:
    metadata:
      labels:
        app: grouping-service
        tier: backend
    spec:
      enableServiceLinks: false
      containers:
      - name: grouping-service
        image: gcr.io/lessgo-490819/grouping-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: http
        envFrom:
        - configMapRef:
            name: lessgo-config
        - secretRef:
            name: lessgo-secrets
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: grouping-service
  namespace: lessgo
  labels:
    app: grouping-service
spec:
  selector:
    app: grouping-service
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
    name: http
  type: ClusterIP
